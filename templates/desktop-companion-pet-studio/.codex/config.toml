# Project defaults; task/UI overrides may take precedence. Preserve host authentication.
model = "gpt-6-astra"
model_reasoning_effort = "low"

[features]
multi_agent = true

[agents]
enabled = true
max_concurrent_threads_per_session = 1
interrupt_message = true
