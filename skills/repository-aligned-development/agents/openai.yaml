interface:
  display_name: "Repository-Aligned Development"
  short_description: "Align changes with each repository's local conventions"
  default_prompt: "Use $repository-aligned-development to inspect this repository, choose the appropriate contract depth, and keep the change aligned through verification."
